sonar.projectKey=NotJuanSe_FedEx_Shipping_Manager
sonar.projectName=FedEx Shipping Manager
sonar.sources=src
sonar.sourceEncoding=UTF-8

# src/components/ui/** es codigo generado por shadcn (npx shadcn add).
# Editarlo se pierde en la siguiente actualizacion del componente,
# asi que se analiza el codigo propio y no el vendorizado.
sonar.exclusions=src/components/ui/**,.next/**,node_modules/**
